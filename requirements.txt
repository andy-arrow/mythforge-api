# Production WSGI server
gunicorn==22.0.0

# Core API
Flask==3.0.3
Werkzeug==3.0.3

# Image generation (Phase 4)
numpy
Pillow==10.3.0

# Transcription (Phase 3+)
faster-whisper==1.0.3
requests           # huggingface_hub 1.x dropped requests as a hard dep; faster-whisper still needs it
