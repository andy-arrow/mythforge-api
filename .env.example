# MythForge environment variables.
# Copy to .env and edit before deploying:  cp .env.example .env

# Public IP of the VPS (used in render response URLs)
VPS_IP=51.83.154.112

# API key for /api/* endpoints. Leave empty to disable auth (open API).
# Example: API_KEY=mysecretkey123
API_KEY=

# Flask environment: production | development
FLASK_ENV=production

# Maximum MP3 upload size in megabytes
MAX_UPLOAD_MB=500

# Maximum total render time in seconds (Whisper + PIL + FFmpeg combined)
# Must be less than gunicorn --timeout (720s)
FFMPEG_TIMEOUT=660

# Auto-delete job directories older than this many hours
JOB_TTL_HOURS=48

# Whisper model size: tiny | base | small | medium | large-v3
# tiny  (~39 MB):  fastest, good for clear narration (~8-15s for 4-min audio on CPU)
# base  (~74 MB):  better accuracy, ~16-30s
# small (~244 MB): excellent accuracy, ~40-80s
WHISPER_MODEL=tiny

# Transcription language (ISO 639-1 code, e.g. en, el, fr).
# Set to 'auto' or leave empty for automatic language detection.
WHISPER_LANGUAGE=en

# Optional: log file path inside container (empty = stdout only)
# LOG_FILE=/app/logs/api.log
LOG_FILE=
